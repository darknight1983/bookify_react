{
  "name": "bookify",
  "version": "1.0.0",
  "description": "A simple book app for viewing items on New York Times bestsellers list.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/app.js",
    "heroku-postbuild": "cd bookify-ui/ && npm install --only=dev && npm install && npm run build"
  },
  "author": "Jacolby Green",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.1"
  }
}
